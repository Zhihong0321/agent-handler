PORT=3000
HOST=0.0.0.0
PERPLEXITY_BASE_URL=https://ee-perplexity-wrapper-production.up.railway.app
# Optional: fallback account_name configured in the wrapper dashboard
# DEFAULT_ACCOUNT_NAME=your_account_name
# Optional: fallback collection/Space UUID
# DEFAULT_COLLECTION_UUID=
# Optional: disable answer-only mode
# ANSWER_ONLY=false
# Optional: basic rate limiting (per customerId)
# RATE_LIMIT_MAX=30
# RATE_LIMIT_WINDOW_MS=60000
# Optional: chunk size for streaming responses (in characters)
# STREAMING_CHUNK_SIZE=800
# Postgres connection (optional; falls back to in-memory if unset/unreachable)
# POSTGRES_URL=postgres://user:pass@localhost:5432/perplexity_agent
# POSTGRES_MAX_CONNECTIONS=10
